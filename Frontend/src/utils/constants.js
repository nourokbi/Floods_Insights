// Theme colors - Adapted for Floods Insights
export const COLORS = {
  primary: "#2563eb", // Blue theme for water/floods
  primaryRgba: "rgba(37, 99, 235, 0.2)",
  primaryDark: "#1e40af",
  textLight: "#e0e0e0",
  textDark: "#2c3e50",
  gridLight: "rgba(0, 0, 0, 0.1)",
  gridDark: "rgba(255, 255, 255, 0.1)",
};

// Map zoom configuration
export const ZOOM_CONFIG = {
  extent: 1.3,
  duration: 1500,
  easing: "ease-in-out",
};

// Search configuration
export const SEARCH_CONFIG = {
  maxSuggestions: 5,
};

// LocalStorage keys
export const STORAGE_KEYS = {
  theme: "floodsInsightsTheme",
};
